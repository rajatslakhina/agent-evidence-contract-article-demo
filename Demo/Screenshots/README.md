# Screenshots

Empty on purpose. The Demo app has not been run on a Simulator yet: this repo was built in a Linux sandbox (library verified with `swift build` and `swift test`), and the scheduled run that produced it was not allowed to drive Xcode or Simulator. No screenshot was faked. If you run it, a PR adding one is welcome.
